#!/usr/bin/env python3
"""Build the canonical vanilla SAC provenance documentation from saved artifacts."""
from __future__ import annotations
import csv,hashlib,json,platform,subprocess,sys
from pathlib import Path
ROOT=Path('/home/brainlab/FaRL'); BASE=ROOT/'results/vanilla_rl_baseline_v1'; DOC=ROOT/'docs'; OUT=DOC/'vanilla_sac_baseline_recipe';OUT.mkdir(parents=True,exist_ok=True)
FIX=BASE/'sac_seed0_300k_fixed_horizon_from_100k';PARENT=BASE/'sac_seed0_100k_fixed_horizon';CTRL=BASE/'sac_seed0_300k_from_last';REWARD=BASE/'sac_seed0_300k_lift_progress';BEST=FIX/'checkpoints/best_model.zip';LAST=FIX/'checkpoints/last_model.zip'
def jload(p):return json.loads(Path(p).read_text())
def dump(n,x):(OUT/n).write_text(json.dumps(x,indent=2,sort_keys=True))
def sha(p):
 h=hashlib.sha256()
 with Path(p).open('rb') as f:
  for b in iter(lambda:f.read(8<<20),b''):h.update(b)
 return h.hexdigest()
def cmd(x):return subprocess.run(x,cwd=ROOT,text=True,stdout=subprocess.PIPE,stderr=subprocess.STDOUT).stdout.strip()
def main():
 final=jload(BASE/'termination_ab_v2/final_summary.json');fc=jload(FIX/'final_model_comparison.json');cc=jload(CTRL/'final_model_comparison.json');rs=jload(BASE/'reward_ab_v1/final_summary.json');status=jload(FIX/'run_status.json');pstatus=jload(PARENT/'run_status.json')
 best=fc['best_model'];last=fc['last_model'];cb=cc['best_model'];cl=cc['last_model']
 env={'task':'Lift','robot':'Panda','gripper':'PandaGripper','controller':'OSC_POSE','controller_config':{'type':'OSC_POSE','input_max':1,'input_min':-1,'output_max':[.05,.05,.05,.5,.5,.5],'output_min':[-.05,-.05,-.05,-.5,-.5,-.5],'kp':150,'damping':1,'impedance_mode':'fixed','kp_limits':[0,300],'damping_limits':[0,10],'position_limits':None,'orientation_limits':None,'uncouple_pos_ori':True,'control_delta':True,'interpolation':None,'ramp_ratio':.2},'control_frequency_hz':20,'horizon_steps':200,'duration_seconds':10,'ignore_done':True,'hard_reset':True,'has_renderer':False,'has_offscreen_renderer':False,'use_camera_obs':False,'use_object_obs':True,'reward_shaping':True,'reward_scale':1.0,'initialization_noise':'robosuite default','placement_sampler':{'type':'UniformRandomSampler','x_range':[-.03,.03],'y_range':[-.03,.03],'rotation':None,'ensure_object_boundary_in_range':False,'ensure_valid_placement':True,'reference_pos':[0,0,.8],'z_offset':.01},'recommended_termination_mode':'fixed_horizon'};dump('final_environment_config.json',env)
 sac={'algorithm':'SAC','policy':'MlpPolicy','actor_architecture':[256,256],'critic_architecture':[256,256],'activation':'torch.nn.ReLU','learning_rate':3e-4,'buffer_size':1000000,'learning_starts':5000,'batch_size':256,'gamma':.99,'tau':.005,'train_freq':1,'gradient_steps':1,'ent_coef':'auto','target_entropy':-7,'device':'cpu','seed':0,'normalization':False};dump('final_sac_config.json',sac)
 obs={'flat_dimension':42,'dtype':'float32','normalization':False,'clipping':False,'camera_or_rgb':False,'keys_in_order':[{'key':'object-state','flat_indices':'0:10','dimension':10,'components':[{'name':'cube_pos','indices':'0:3','dimension':3},{'name':'cube_quat','indices':'3:7','dimension':4},{'name':'gripper_to_cube_pos','indices':'7:10','dimension':3}]},{'key':'robot0_proprio-state','flat_indices':'10:42','dimension':32,'components':[{'name':'joint_pos_cos','indices':'10:17'},{'name':'joint_pos_sin','indices':'17:24'},{'name':'joint_vel','indices':'24:31'},{'name':'eef_pos','indices':'31:34'},{'name':'eef_quat','indices':'34:38'},{'name':'gripper_qpos','indices':'38:40'},{'name':'gripper_qvel','indices':'40:42'}]}],'observable':['EEF state','gripper state','cube position and orientation','gripper-to-cube relative position'],'not_observable':['failure labels','failure classifier output','hidden physics parameters','future trajectory']};dump('observation_schema.json',obs)
 action={'dimension':7,'dtype':'float32','space':'Box','low':[-1]*7,'high':[1]*7,'ordering':['dx','dy','dz','d_axis_angle_x','d_axis_angle_y','d_axis_angle_z','gripper'],'controller_outputs':{'translation_m':[-.05,.05],'rotation_rad':[-.5,.5]},'gripper_sign':'positive closes; negative opens','sb3':'SAC squashed Gaussian is mapped to Box bounds; wrapper clips once','audit':'No double scaling, gripper-sign inversion, or bounds mismatch found.'};dump('action_schema.json',action)
 success={'source':'/workspace/collectenv/lib/python3.9/site-packages/robosuite/environments/manipulation/lift.py:_check_success','exact_expression':'cube_height > table_height + 0.04','table_height_m':.8,'threshold_m':.84,'grasp_required':False,'persistence_required':False,'velocity_constraint':False,'primary_metric':'ever_success','secondary_metric':'final_success'};dump('success_definition.json',success)
 reward={'mode':'control/original robosuite Lift shaped reward','source':'/workspace/collectenv/lib/python3.9/site-packages/robosuite/environments/manipulation/lift.py:193-242','unscaled':{'success':'2.25 if success; replaces shaped components','reaching':'1-tanh(10*distance) before success','grasp':'0.25 if grasped before success'},'scaling':'reward *= reward_scale / 2.25','reward_scale':1.0,'limitation':'No continuous lift-progress component before the 4 cm success threshold.','excluded_treatment':{'name':'signed_lift_progress','verdict':rs['LIFT_REWARD_EFFECT'],'best_success':rs['TREATMENT_SUCCESS_BEST'],'last_success':rs['TREATMENT_SUCCESS_LAST']}};dump('reward_definition.json',reward)
 wrapper={'robosuite_done':'ignored because ignore_done=True','control_success':{'is_success':True,'terminated':True,'truncated':False},'fixed_success_before_horizon':{'is_success':True,'terminated':False,'truncated':False},'horizon':{'terminated':False,'truncated':True},'sb3_timeout':'DummyVecEnv records TimeLimit.truncated; ReplayBuffer samples done*(1-timeouts)','fixed_horizon_bootstrap':True,'success_termination_bootstrap':False};dump('final_wrapper_semantics.json',wrapper)
 freeze=cmd([str(ROOT/'.venv_rl/bin/pip'),'freeze']);(OUT/'dependency_manifest.txt').write_text('# Generated from .venv_rl/bin/pip freeze\n# robosuite resolves from /workspace/collectenv site-packages and is pinned below.\n'+freeze+'\n')
 runtime={'generated_at':'2026-08-30 UTC','host_architecture':'aarch64','kernel':platform.release(),'python':sys.version.splitlines()[0],'venv':str(ROOT/'.venv_rl'),'training_device':'cpu','versions':{'robosuite':'1.2.0; pip provenance git cheng-chi/robosuite@277ab9588ad7a4f4b55cf75508b44aa67ec171f0','robomimic':'NOT_INSTALLED_NOT_REQUIRED','free-mujoco-py':'2.1.6','MuJoCo_path_version':'/workspace/mujoco-2.1.1','numpy':'1.23.3','scipy':'1.9.1','torch':'2.8.0+cpu','stable-baselines3':'2.7.1','gymnasium':'1.1.1','gym':'NOT_INSTALLED'},'environment_variables':{'PYGLFW_LIBRARY':'/workspace/collectenv/lib/libglfw.so.3','MUJOCO_PY_MUJOCO_PATH':'/workspace/mujoco-2.1.1','MUJOCO_GL':'osmesa','LD_LIBRARY_PATH_prefix':['/workspace/collectenv/lib','/workspace/mujoco-2.1.1/bin','/workspace/mujoco-2.1.1/lib']},'performance_note':'Low-dimensional MLP SAC is CPU-valid and primarily environment/MuJoCo-step limited.'};dump('environment_manifest.json',runtime)
 source_files=['rl_baselines/envs.py','rl_baselines/train_sac.py','rl_baselines/train_sac_resume.py','rl_baselines/evaluate.py','rl_baselines/callbacks.py','rl_baselines/progress.py','rl_baselines/policy_adapter.py','scripts/run_vanilla_sac.sh','scripts/run_vanilla_sac_from_checkpoint.sh','scripts/run_fixed_horizon_300k_continuation.sh','scripts/watch_vanilla_sac.sh']
 prov={'git':{'branch':cmd(['git','branch','--show-current']),'head':cmd(['git','rev-parse','HEAD']),'remote':cmd(['git','remote','-v']) or 'NONE','status':cmd(['git','status','--short']),'tracked_diff':cmd(['git','diff','--name-only']) or 'NONE'},'files':[{'path':x,'sha256':sha(ROOT/x)} for x in source_files],'external_local_source':{'robosuite_lift':'/workspace/collectenv/lib/python3.9/site-packages/robosuite/environments/manipulation/lift.py','original_robotics_assets':'/home/robotics (read-only)'}};dump('source_provenance.json',prov)
 good=[{'designation':'RECOMMENDED_DOWNSTREAM_NOMINAL_POLICY','path':str(BEST),'run':FIX.name,'effective_step':280000,'sha256':sha(BEST),'termination':'fixed_horizon','reward':'original shaped Lift','seed':0,'evaluation':{'ever_success':best['success_rate'],'final_success':best['final_success_rate'],'lift':best['lift_rate'],'p_lift_given_grasp':best['p_lift_given_grasp']},'use':'deterministic nominal closed-loop policy; seed-0 only'},{'designation':'STABLE_LAST_REFERENCE','path':str(LAST),'run':FIX.name,'effective_step':300000,'sha256':sha(LAST),'termination':'fixed_horizon','reward':'original shaped Lift','seed':0,'evaluation':{'ever_success':last['success_rate'],'final_success':last['final_success_rate'],'lift':last['lift_rate'],'p_lift_given_grasp':last['p_lift_given_grasp']},'use':'late-training stability reference'}];dump('known_good_checkpoints.json',good)
 nongood=[{'role':'diagnostic parent; not nominal','path':str(BASE/'sac_seed0_100k_diag/checkpoints/last_model.zip'),'reason':'success-termination, 5% success'},{'role':'control grasp-plateau','path':str(CTRL/'checkpoints/best_model.zip'),'reason':'0% success, 2% lift at final matched evaluation'},{'role':'control late plateau','path':str(CTRL/'checkpoints/last_model.zip'),'reason':'0% success/lift'},{'role':'reward-treatment diagnostic','path':str(REWARD/'checkpoints/best_model.zip'),'reason':'signed lift-progress is not the final reward and regressed'}];
 for x in nongood:
  x['sha256']=sha(x['path']) if Path(x['path']).exists() else 'UNKNOWN'
 dump('known_bad_or_nonfinal_checkpoints.json',nongood)
 history=[['phase','run','steps','semantics','reward','purpose','result','verdict'],['A','sac_20k_sanity / sac_20k_sanity_tqdm','20k','success termination','original','pipeline sanity','numerical updates; task not learned','PASS_NUMERIC_ONLY'],['B','sac_seed0_100k_diag','100k fresh','success termination','original','initial control','best 10%, last 5%; reach/grasp learned','PARTIAL_LEARNING'],['C','sac_seed0_300k_from_last','100k->300k continuation','success termination','original','budget test','best/last success 0%; grasp 100%/95%','PLATEAU_AT_GRASP'],['D','sac_seed0_300k_lift_progress','300k fresh','success termination','signed lift progress','reward A/B','best 5%, last 1%; unstable','NOT_SUPPORTED_WITH_THIS_SHAPING'],['E1','sac_seed0_100k_fixed_horizon','100k fresh','fixed horizon','original','termination A/B parent','best 7%, last 6%','VALID_FIXED_PARENT'],['E2','sac_seed0_300k_fixed_horizon_from_100k','100k->300k true continuation','fixed horizon','original','termination A/B','best 92%, last 88%','FIXED_HORIZON_SUPPORTED']]
 with (OUT/'experiment_history.csv').open('w',newline='') as f:csv.writer(f).writerows(history)
 commands='''# Command reference\n\n## Fresh run\n\n```bash\ncd /home/brainlab/FaRL\n./scripts/run_vanilla_sac.sh <steps> <seed> <run_name> [--termination-mode success|fixed_horizon] [--reward-mode control|signed_lift_progress]\n```\n\nExact successful parent command (from `run_status.json`):\n\n```bash\n./scripts/run_vanilla_sac.sh 100000 0 sac_seed0_100k_fixed_horizon --termination-mode fixed_horizon\n```\n\nExact continuation helper:\n\n```bash\n./scripts/run_fixed_horizon_300k_continuation.sh\n```\n\nThis helper uses the saved fixed 100k last model **and replay buffer**, target 300k, seed 0, and `fixed_horizon`. Do not rerun into an existing result directory.\n\n## Monitor / inspect / plot\n\n```bash\n./scripts/watch_vanilla_sac.sh\n./scripts/watch_vanilla_sac.sh <run_name>\n./scripts/inspect_vanilla_sac.sh\n.venv_rl/bin/python -m rl_baselines.plot_run --run results/vanilla_rl_baseline_v1/<run_name>\n.venv_rl/bin/python -m rl_baselines.audit_env\n.venv_rl/bin/python -m rl_baselines.reward_audit\n```\n\nThere is no single verified command that recreates the complete two-stage provenance without overwriting names. Use new run names and first create a fresh fixed-horizon 100k parent, then invoke `run_vanilla_sac_from_checkpoint.sh <parent-last> 300000 <seed> <new-name> 100000 fixed_horizon`.\n''';(OUT/'command_reference.md').write_text(commands)
 artifacts='''# Artifact index\n\n| Item | Path | Purpose / verdict | Key result |\n|---|---|---|---|\n| 20k sanity | `results/vanilla_rl_baseline_v1/sac_20k_sanity_tqdm/` | SAC numerical pipeline | `summary.json`; PASS_NUMERIC_ONLY |\n| 100k control | `results/vanilla_rl_baseline_v1/sac_seed0_100k_diag/` | Original success-termination baseline | `final_model_comparison.json`; best 10%, last 5% |\n| 300k control | `results/vanilla_rl_baseline_v1/sac_seed0_300k_from_last/` | Budget continuation | `final_300k_analysis.md`; PLATEAU_AT_GRASP |\n| Forensics | `results/vanilla_rl_baseline_v1/live_training_forensic_audit_v1/` | interface/numerical audit | `final_report.md` |\n| Reward A/B | `results/vanilla_rl_baseline_v1/reward_ab_v1/` | signed lift-progress | `final_summary.json`; NOT_SUPPORTED |\n| Fixed 100k parent | `results/vanilla_rl_baseline_v1/sac_seed0_100k_fixed_horizon/` | fresh fixed-horizon parent | `run_status.json`, checkpoints/replay |\n| Fixed 300k | `results/vanilla_rl_baseline_v1/sac_seed0_300k_fixed_horizon_from_100k/` | successful true continuation | `final_model_comparison.json`; best 92%, last 88% |\n| Termination A/B | `results/vanilla_rl_baseline_v1/termination_ab_v2/` | causal screen | `final_report.md`; FIXED_HORIZON_SUPPORTED |\n''';(OUT/'artifact_index.md').write_text(artifacts)
 checklist='''# Reproduction checklist\n\n- [ ] `.venv_rl` imports and dependency versions match the manifest\n- [ ] MuJoCo 2.1.1 path, OSMesa, GLFW, and LD_LIBRARY_PATH are set\n- [ ] Environment reset/step smoke passes\n- [ ] Observation is float32, shape `(42,)`, ordered object then proprio\n- [ ] Action is 7-D Box[-1,1] and positive gripper closes\n- [ ] Success is cube z > table z + 0.04 m\n- [ ] Fixed success does not terminate; step 200 truncates\n- [ ] Scripted controller still solves Lift\n- [ ] SAC config exactly matches `final_sac_config.json`\n- [ ] Fresh parent uses fixed horizon from step 0\n- [ ] Parent `last_model.zip` and `replay_buffer.pkl` are both present\n- [ ] Continuation restores model, optimizers, entropy, timestep, replay\n- [ ] tqdm and CSV metrics agree\n- [ ] Final deterministic seeds are 900000–900099\n- [ ] Best and last both receive 100-episode evaluation\n- [ ] Checkpoint SHA-256 values match\n- [ ] Policy actions are recomputed from the current observation\n''';(OUT/'reproduction_checklist.md').write_text(checklist)
 trouble='''# Troubleshooting\n\n- **Return and grasp rise, lift stays zero:** inspect fixed-horizon semantics, P(lift|grasp), and post-grasp z action. Do not tune the network first.\n- **Scripted controller fails:** treat this as an environment/controller/runtime problem.\n- **Scripted succeeds but SAC never reaches:** audit observation ordering, action scaling/sign, reward, and resets.\n- **SAC grasps under success termination but will not lift:** verify a one-variable fixed-horizon run before entropy or architecture changes.\n- **Fixed horizon succeeds:** do not reintroduce reward shaping without a separate causal need.\n- **tqdm shows completed immediately:** inspect `run_status.json` and requested total; ensure a new run name.\n- **Concurrent-run refusal:** inspect `active_run.json` and whether its PID exists; never bypass a genuinely running job.\n- **Continuation performs unlike parent:** confirm replay buffer was loaded and `reset_num_timesteps=False`; model-only loading is warm-start.\n- **MuJoCo import/GL error:** export the exact variables in `environment_manifest.json`; use the project launcher.\n''';(OUT/'troubleshooting.md').write_text(trouble)
 summary={'BASELINE_NAME':'FaRL vanilla SAC fixed-horizon seed0 baseline','TASK':'robosuite Lift','ROBOT':'Panda','CONTROLLER':'OSC_POSE','CONTROL_FREQ':20,'HORIZON':200,'OBSERVATION_TYPE':'low-dimensional state; no RGB','OBS_DIM':42,'ACTION_DIM':7,'REWARD_MODE':'original robosuite Lift dense shaped reward','REWARD_SCALE':1.0,'SUCCESS_CRITERION':'cube_height > table_height + 0.04 m','TERMINATION_MODE':'fixed_horizon','ALGORITHM':'SAC','POLICY':'MlpPolicy','ACTOR_ARCH':[256,256],'CRITIC_ARCH':[256,256],'LEARNING_RATE':3e-4,'BUFFER_SIZE':1000000,'LEARNING_STARTS':5000,'BATCH_SIZE':256,'GAMMA':.99,'TAU':.005,'TRAIN_FREQ':1,'GRADIENT_STEPS':1,'ENT_COEF':'auto','TARGET_ENTROPY':-7,'DEVICE':'cpu','SEED0_PARENT_RUN':PARENT.name,'SEED0_PARENT_STEPS':100000,'SEED0_FIXED_RUN':FIX.name,'SEED0_EFFECTIVE_TOTAL_STEPS':300000,'SEED0_BEST_CHECKPOINT':str(BEST),'SEED0_BEST_CHECKPOINT_SHA256':sha(BEST),'SEED0_BEST_EVER_SUCCESS':best['success_rate'],'SEED0_LAST_EVER_SUCCESS':last['success_rate'],'SEED0_BEST_FINAL_SUCCESS':best['final_success_rate'],'SEED0_LAST_FINAL_SUCCESS':last['final_success_rate'],'SEED0_BEST_LIFT':best['lift_rate'],'SEED0_P_LIFT_GIVEN_GRASP':best['p_lift_given_grasp'],'MULTISEED_STATUS':'PENDING_SEEDS_1_AND_2','RECOMMENDED_DOWNSTREAM_POLICY':str(BEST),'FRESH_FIXED_HORIZON_FROM_SCRATCH_VALIDATED':True,'STATUS':'SEED0_WORKING_BASELINE_ESTABLISHED'};dump('recipe_summary.json',summary)
 dump('documentation_validation.json',{'CONFIG_VALUES_VERIFIED':'YES_SOURCE_AND_RUNTIME','CHECKPOINT_PROVENANCE_VERIFIED':'YES_RUN_STATUS_PARENT_HASH_AND_REPLAY','RESULT_VALUES_VERIFIED':'YES_FINAL_MODEL_COMPARISON_AND_PAIRED_STATISTICS','COMMANDS_VERIFIED':'YES_HELP_SHELL_SYNTAX_AND_COMPLETED_RUN_STATUS','HASHES_VERIFIED':'YES_RECOMPUTED_SHA256','POLICY_CLOSED_LOOP_SMOKE':'PASS_OBSERVATION_AND_ACTION_CHANGED_ACTIONS_IN_BOUNDS','UNRESOLVED_ITEMS':['Seeds 1 and 2 not run','Fresh fixed-horizon 300k one-stage run not run; two-stage fixed semantics from step 0 is validated','Throw/overshoot/behavior-quality video audit not completed','Exact original collector env_kwargs vary by recorded dataset episode and are not reduced to one universal config','RNG streams were not separately checkpointed at 100k continuation boundary']})
 main_doc=f'''# FaRL vanilla SAC baseline recipe\n\n> **Working recipe — status: `SEED0_WORKING_BASELINE = ESTABLISHED`; `MULTISEED_REPLICATION = PENDING`.** Use robosuite Lift, Panda/PandaGripper, OSC_POSE at 20 Hz, 42-D low-dimensional state, SAC MLP 256×256, original shaped Lift reward, fixed-horizon semantics, horizon 200, and 300k effective interactions. Load `{BEST}` for downstream deterministic nominal control. Primary metric: `ever_success`. This is a seed-0 baseline, not a robust multiseed claim.\n\n## 1. Purpose and scope\n\nThis policy supplies a nominal closed-loop task policy, a control for failure-aware recovery, feedback continuation after interventions, and a failure generator under A/B/C perturbations. It is **not failure-conditioned** and must not receive classifier output, ground-truth failure labels, hidden physics, or future trajectory data.\n\n## 2. Proven status and the critical provenance correction\n\nThe successful chain was **not** the success-termination 100k control followed by a semantic switch. Saved run metadata proves this chain:\n\n`fresh fixed horizon 0→100k` (`{PARENT.name}`) → 100k `last_model.zip` + 100k replay → `fixed horizon 100k→300k` true continuation (`{FIX.name}`).\n\nThus fixed-horizon semantics were used from step 0 through 300k. The two-stage continuation restored actor, critics/targets, optimizers, entropy state, timestep, and replay. It added 200k transitions and 200k updates. Python/NumPy/environment RNG streams were not separately restored. A monolithic one-stage command was not run, but **fresh fixed horizon from random initialization is validated through the two-stage trajectory**.\n\n## 3. Runtime\n\nKnown-good host is aarch64, Python 3.9.23. Training used CPU: torch 2.8.0+cpu, SB3 2.7.1, Gymnasium 1.1.1, NumPy 1.23.3, SciPy 1.9.1, robosuite 1.2.0 and free-mujoco-py 2.1.6 against `/workspace/mujoco-2.1.1`. Robomimic and legacy Gym are not required. `.venv_rl` isolates modern SB3/Gymnasium while exposing historical robosuite dependencies under `/workspace/collectenv`. Required OSMesa/GLFW/MuJoCo variables are exact in `environment_manifest.json`. Low-dimensional SAC is CPU-valid and environment-step limited.\n\n## 4. Environment\n\n`rl_baselines/envs.py` builds Lift with Panda, PandaGripper, OSC_POSE, object observations enabled, cameras disabled, shaped reward scale 1, 20 Hz, horizon 200, `ignore_done=True`, and `hard_reset=True`. Default cube placement is uniform x/y ±0.03 m around table offset `[0,0,0.8]`, z offset 0.01. Controller translation commands map to ±0.05 m and rotation to ±0.5 rad.\n\nThe original FaRL collector (`/home/robotics/.../a_cluttered.py`) is multimodal and enables offscreen agent/wrist cameras; it accepts recorded `env_kwargs`. Vanilla RL intentionally uses nominal state-only observations and no rendering. `/home/robotics` and `/workspace/collectenv` remain read-only.\n\n## 5. Observation and action\n\nObservation is float32 `(42,)`: `object-state` indices 0:10 then `robot0_proprio-state` 10:42. It contains cube pose and relative position plus joint, EEF, and gripper state. There is no normalization, clipping, RGB, failure label, or hidden physics. See `observation_schema.json`.\n\nAction is `[dx,dy,dz,dRx,dRy,dRz,gripper]`, Box[-1,1]^7. Positive gripper closes. SAC uses a squashed Gaussian mapped to these bounds; the wrapper clips once. Audits found no double scaling, sign inversion, or bound mismatch.\n\n## 6. Success and reward\n\nLocal robosuite `_check_success()` is exactly `cube_height > table_height + 0.04`; here the table is 0.8 m. Grasp, persistence, and low velocity are not required. Report `ever_success` primarily and `final_success` secondarily.\n\nBefore success the original shaped reward is `(1-tanh(10*distance) + 0.25*grasp)/2.25`; success replaces this with `2.25/2.25 = 1`. There is no continuous pre-threshold lift term. The signed lift-progress experiment reached best 5%, last 1% and is `NOT_SUPPORTED_WITH_THIS_SHAPING`; it is excluded.\n\n## 7. Termination semantics\n\nOriginal control turns success into `terminated=True`; horizon is `truncated=True`. Fixed horizon keeps `is_success=True` but returns `terminated=False,truncated=False` until step 200, which truncates. SB3 masks timeout dones and bootstraps through horizons. It also bootstraps through fixed-horizon success but not control terminal success. This matches the general robosuite fixed-horizon convention, with local code authoritative.\n\n## 8. Why termination mattered\n\nAt matched 300k best checkpoints and 100 seeds, control had success 0%, grasp 100%, lift 2%, P(lift|grasp) 2%, mean lift delta 0.310 mm. Fixed best had ever-success **92%**, final-success 74%, grasp 93%, lift **92%**, P(lift|grasp) **98.9%**. Fixed last remained 88% success, 74% final-success, 94% grasp, 90% lift. Paired differences were +92 points (95% CI +86 to +97) for success and +90 (+84 to +95) for lift. Verdict: `FIXED_HORIZON_SUPPORTED` for this single-seed causal screen.\n\nMechanistic observations, not a causal decomposition: reward≈1 replay proxy rose from 0.0163% to 23.88%; post-grasp mean z action shifted −0.865→−0.587 and positive-z incidence increased. Fixed horizon jointly changes bootstrap targets, successful-state replay occupancy, and repeated post-success reward.\n\n## 9. SAC and numerical health\n\nSAC `MlpPolicy`, actor/critic 256×256 ReLU, lr 3e-4, replay 1e6, learning starts 5k, batch 256, gamma .99, tau .005, train/update frequency 1/1, `ent_coef=auto`, target entropy −7, CPU, no normalization. Actor/critic/entropy remained finite, parameters updated, replay and timeout handling were valid. Falling alpha alone was consistent with normal autotuning and is not evidence of collapse; do not manually tune entropy by default.\n\n## 10. Evaluation and checkpointing\n\nPolicies are deterministic for evaluation. Online evaluation uses 20 episodes at 10k cadence with seeds `700000 + step + episode_index`; final best/last use 100 matched seeds 900000–900099. Selection tuple is success, lift, median physical lift, grasp, then return. Never select by shaped return alone: the failed control reached 95–100% grasp while success stayed 0%. Best fixed checkpoint is effective step 280k; last is 300k.\n\n## 11. Monitoring and behavioral funnel\n\nMonitor return, ever/final success, grasp rate/duration, lift, P(lift|grasp), lift mm, actor/critic loss, alpha, action norm/saturation, translation/rotation/gripper statistics, and post-grasp z action. The tqdm callback displays step/total, ETA/fps and the same core fields persisted in `live_metrics.csv`; evaluations use `tqdm.write` and persist to `learning_curve.csv`. Diagnose Reach→Contact→Grasp→Lift→Success, not return alone.\n\n## 12. Historical debugging path\n\n| Phase | Actual experiment | Lesson |\n|---|---|---|\n| A | 20k sanity | numerical SAC works; `PASS_NUMERIC_ONLY` |\n| B | 100k success-termination control | best 10%, last 5%; reach/grasp only |\n| C | control true continuation to 300k | 0% success with 95–100% grasp; `PLATEAU_AT_GRASP` |\n| D | fresh 300k signed lift-progress | small intermittent gain, regression; not supported |\n| E | fresh fixed 100k + fixed continuation to 300k | best 92%, last 88%; supported |\n\n## 13. Loading and closed-loop use\n\n```python\nfrom rl_baselines.envs import LiftGymnasium\nfrom rl_baselines.policy_adapter import VanillaRLPolicy\n\nenv = LiftGymnasium(seed=0, horizon=200, termination_mode="fixed_horizon")\npolicy = VanillaRLPolicy("{BEST}", algorithm="SAC", device="cpu")\npolicy.reset()\nobs, info = env.reset(seed=0)\nfor _ in range(200):\n    action = policy.act(obs, deterministic=True)\n    obs, reward, terminated, truncated, info = env.step(action)\n    if terminated or truncated:\n        break\nenv.close()\n```\n\nThe required loop is current `obs_t → policy → action_t → env.step → obs_(t+1)`. Never splice a recorded historical action suffix after recovery. A real checkpoint smoke showed changed observations produced changed in-bound actions; `tests/test_policy_adapter_closed_loop.py` guards this contract. Normalization is absent, so there is no VecNormalize state to load.\n\n## 14. Continuation and run registry\n\nA true SAC continuation requires the model zip (policy, targets, optimizers, entropy, timestep) **and replay buffer**; normalization state would also be required if used. Model-only loading is a warm-start. `active_run.json` identifies the latest registered run; managers reject a live concurrent PID and append terminal records to `run_history.jsonl`. Run directories contain `run_status.json`, `live_metrics.csv`, `learning_curve.csv`, `episode_metrics.csv`, `summary.json`, final comparisons, and `checkpoints/{{best,last,latest}}_model.zip` plus replay where saved.\n\n## 15. Common failure modes — do not repeat\n\n1. Do not judge learning from shaped return alone.\n2. Do not call grasp task completion.\n3. Do not call low alpha exploration collapse by itself.\n4. Do not change reward, termination, entropy, and architecture together.\n5. Do not report only last checkpoint.\n6. Do not call model-only loading true continuation.\n7. Do not encode horizon as true termination.\n8. Do not terminate a fixed-horizon episode at success.\n9. Do not use recorded action suffixes as closed-loop feedback.\n10. Do not claim signed lift-progress solved Lift.\n11. Do not claim seed-0 92% is multiseed.\n\n## 16. Forward recommendation and limitations\n\nUse the best fixed-horizon seed-0 checkpoint, low-dimensional observations, original reward, 200-step fixed horizon, OSC_POSE 20 Hz, 256×256 SAC, and automatic entropy for downstream FaRL development. Seeds 1 and 2 remain pending. The very large 0.65–0.72 m maximum lift deltas satisfy official Lift success but need a secondary throw/overshoot/stability quality audit. Do not invalidate the official result without that evidence.\n\nReplication status: control seed 0 completed (best 0%, last 0%); fixed seed 0 completed (best 92%, last 88%, best lift 92%, P(lift|grasp) 98.9%). Control/fixed seeds 1 and 2 are `PENDING`.\n\n## 17. Commands and indexes\n\nExact commands are in [command_reference.md](vanilla_sac_baseline_recipe/command_reference.md), artifacts in [artifact_index.md](vanilla_sac_baseline_recipe/artifact_index.md), checklist in [reproduction_checklist.md](vanilla_sac_baseline_recipe/reproduction_checklist.md), and troubleshooting in [troubleshooting.md](vanilla_sac_baseline_recipe/troubleshooting.md). Machine-readable values and hashes are in the companion directory.\n\n## 18. Git/source provenance\n\nRepository branch `{prov['git']['branch']}`, HEAD `{prov['git']['head']}`. The working tree contains extensive untracked project outputs/code; no commit or push was performed. Per-file hashes and full status are captured in `source_provenance.json`.\n''';(DOC/'vanilla_sac_baseline_recipe.md').write_text(main_doc)
 print(json.dumps({'DOCUMENT':str(DOC/'vanilla_sac_baseline_recipe.md'),'RECIPE_DIR':str(OUT),'RECOMMENDED_POLICY':str(BEST),'RECOMMENDED_POLICY_SHA256':sha(BEST),'SEED0_BEST_SUCCESS':best['success_rate'],'SEED0_LAST_SUCCESS':last['success_rate'],'MULTISEED_STATUS':'PENDING_SEEDS_1_AND_2','FRESH_FIXED_HORIZON_FROM_SCRATCH_VALIDATED':'YES','NO_LONG_TRAINING_RUNS_EXECUTED':'YES'},indent=2))
if __name__=='__main__':main()
